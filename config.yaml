MadPlotter:
  # -n, --count arg      Number of plots to create (default = 1, -1 = infinite)
  # -r, --threads arg    Number of threads (default = 4)
  # -u, --buckets arg    Number of buckets (default = 256)
  # -v, --buckets3 arg   Number of buckets for phase 3+4 (default = buckets)
  # -t, --tmpdir arg     Temporary directory, needs ~220 GiB (default = $PWD)
  # -2, --tmpdir2 arg    Temporary directory 2, needs ~110 GiB [RAM] (default = <tmpdir>)
  # -d, --finaldir arg   Final directory (default = <tmpdir>)
  # -p, --poolkey arg    Pool Public Key (48 bytes)
  # -f, --farmerkey arg  Farmer Public Key (48 bytes)
  # -G, --tmptoggle      Alternate tmpdir/tmpdir2 (default = false)
  #     --hwinfo         Print information regarding underlying hardware
  #     --help           Print help
  # Note: tmp and destination path must have trail "/"
  PlotterPath: '/home/username/madmax/build/chia_plot'
  r: 16
  u: 
  v: 
  t: '/mnt/tmp1/'
  2: '/mnt/tmp2/'
  d:
    - '/mnt/disk1/'
    - '/mnt/disk2/'
  p: 'ad1f1da3c152e8d27c9565f35c60b9981a9034ab06a3c59db18efe41ae4255fb88f4605c51bf05d3856461f63a342631'
  f: 'b5bdf57ebf4c5a02264ae7573a2a8be24cf06aa69ed89a7a4bcffb4e048f8132a8c72c226f193961583b818c958fbf6d'
  G:

Main:
  PlotSize: 108797952330 # MadMax plot's size in bytes